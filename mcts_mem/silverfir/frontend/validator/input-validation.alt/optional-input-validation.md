- Semantic validation is an optional build feature, disabled by default even
  for safe module loading; decoder checks remain enabled.

## Moves

- 2026-09-07 (7d1f0c8c) replaced by [[input-validation]]: a safe public loader must not rely on the embedder enabling semantic validation (sourced).
