commit: fc26a6a9

Execution performance is the user's priority, first on the fixed all20 wasmi corpus and exact wasmi CoreMark. Interpreter startup and execution follow, with benchmark-specific fusion forbidden; generic transformations are allowed. The user requested ending speculative JIT work after diminishing returns. No measurement establishes universal No.1 performance.

The last fully passing complete dev revision before inlining is 42396c9d, run [34071064270](https://github.com/mbbill/Silverfir-nano/actions/runs/34071064270), with all 34 actual jobs audited. Its AMD EPYC 7763 all20 projection is -1.1107% versus V8 and +9.7640% versus Cranelift. Applying the bounded-inliner same-model Nano differential projects +0.9903% versus V8 and +12.0960% versus Cranelift. These multiply historical same-CPU-model anchors and revision differentials; they are not a fresh simultaneous three-engine comparison and do not establish Intel rankings. CoreMark remains approximately level/slightly ahead of V8 and 8–9% behind Cranelift. Inlining does not alter CoreMark generated code.

Competitor builds are intentionally infrequent manual controls. Frozen anchor run [34026700835](https://github.com/mbbill/Silverfir-nano/actions/runs/34026700835) fixes all20, and [34028386243](https://github.com/mbbill/Silverfir-nano/actions/runs/34028386243) fixes exact CoreMark. Subsequent iteration uses same-host Nano pairs. Source identity, CPU model, workload inputs and all rows must travel with every result; Rosetta tests establish correctness only, and one CPU model must not be silently substituted for another.

Retained native changes use LEA for non-destructive sums while preserving favorable two-operand immediates, width- and position-proved result flags, truncated equality comparisons, BMI2 shifts/rotates with feature fallback, AMD-specific BEXTR and Intel-specific scalar-conversion dependency breaking, short backward branches, duplicate-heavy jump-table membership branches, scalar early-return frame deferral, and reuse of the dying rhs for commutative scalar floats. Shared changes recover semantically guarded byte-copy loops, cache repeatedly read full native frame words in natural loops, eliminate unobserved overwritten/final frame stores, and remove provably unnecessary compiler scans and allocations. Correctness prerequisites retain memory64/template frame fixes, ARM constant scratch staging, and synchronization of signal-trap lookups with code registration.

The experiment history and all-row tables are in docs/x64-campaign-2026-09-06/EXPERIMENT_LOG.md. This evidence is intentionally not a sum of every attractive microbenchmark delta: later combinations changed profitability, and rejected work contributes zero to final standings. Routine CI never builds V8 or Cranelift as a consequence of a PR/main push; the standalone standings workflow is manual.

Do not repeat the following proposals without a new causal reason. Cross-edge frame forwarding and rematerialization repeatedly added edge work or compiler cost without reliable execution benefit; 6201a516 regressed CoreMark 2.66% on AMD 7763 and 3.52% on Intel 8573C, and lean a09afd24 regressed all eight measured startup points. Reusing residency-analysis scratch cut allocation traffic but still failed to recover native startup. Full-word single-read recurrence caching and enlarged cache banks did not justify their pressure/compile costs. Dead-input register coalescing could remove moves but regressed real workloads. Detailed cache rejection evidence is attached to [[loop-frame-cache]].

Native encoding hypotheses also failed: narrower frame ALU forms, broad memory updates, omitting leaf alignment shims, narrow memory equality, MOVZX mask selection, shifted-operand commutation, RORX shifted forms, cycle-only alignment, uncached mem0 length, call-return status flags, table-index bounds elimination, internal-entry alignment, immediate IMUL and conditional fallthrough. Some failed repeated CoreMark draws; others failed full20 or offered no repeatable benefit. Fewer instructions, fewer static loads or smaller code alone were not acceptance evidence. The x86 backend's attached evidence records representative CPU-specific results.

The retained inliner is the exception backed by a large recursive gain and explicit user acceptance of a small absolute startup cost, not an invitation to accept every marginal rewrite. A local bank-reuse follow-up and two interpreter candidates remain shelved/unmerged. API redesign, crate splitting and crates.io publication are separate later work; no release-readiness claim follows from performance validation.
