---
commit: da94bda8
---
The project's first execution code — the const-expression evaluator for
global inits and segment offsets — already executes by decoding raw bytes
as it goes, before any function interpreter exists. The in-place style was
the house style from the first evaluator ever written.
