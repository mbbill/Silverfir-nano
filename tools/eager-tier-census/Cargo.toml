[package]
name = "sf-nano-eager-tier-census"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sf-nano-core = { path = "../../sf-nano-core", default-features = false, features = ["interp"] }

[dev-dependencies]
wat = "1.246.2"
